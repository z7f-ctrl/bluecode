# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/      # pip install . 构建产物
dist/

# 含本地环境细节/历史 key 前缀，不入库（本地 AI 开发文档）
AGENTS.md

# 含硬编码 API key，不提交（轮换后如确认干净可移出）
test2.py

# 演示/学习脚本（不入库）
hello.py
test1.py

# 环境/密钥（绝不入库）
.env
.env.*
# 但 .env.example 作为可提交的配置模板保留（放在 .env.* 之后才能覆盖前面的忽略）
!.env.example

# 编辑器/OS
.DS_Store
.idea/
.vscode/
*.swp

# benchmark 生成物（任务目录由 prepare.py 生成，结果文件每次跑新增）
benchmarks/quixbugs/tasks/
benchmarks/quixbugs/results/
